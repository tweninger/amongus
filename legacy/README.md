# Legacy Code

This directory keeps old entrypoints and requirements files that are no longer
part of the active local or dsg7 deployment path.

The supported server entrypoint is:

```text
amongus_server.main:app
```

Use the root `pyproject.toml` for dependencies and the root `README.md` /
`DEPLOYMENT.md` for current setup instructions.
