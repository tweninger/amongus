# Legacy Requirements Files

These requirements files are snapshots from older workflows. They are not the
source of truth for local development or deployment.

Use the root `pyproject.toml` instead:

```bash
pip install -e ./among-agents
pip install -e ".[dev]"
```
